'''
As python modules nothing more are than python files, allfiles created during these course are modules ready to be used

'''
#importing a own function
from sum_numbers import sum_number

print(sum_number([1, 2, 3, 4, 5, 6]))